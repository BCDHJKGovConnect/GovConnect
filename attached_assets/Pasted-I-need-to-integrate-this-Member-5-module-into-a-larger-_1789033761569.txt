I need to integrate this Member 5 module into a larger SIH team GitHub repository.

Member 1 and Member 4 are developing their modules in VS Code, while I developed this module in Replit.

Please prepare my project for GitHub sharing and local development in VS Code.

Requirements:

1. Keep all four services:
   - Identity Service
   - Property Service
   - Revenue Service
   - Connector Framework

2. Keep the existing TypeScript + Express implementation.

3. Make sure the project can be cloned from GitHub and run on a normal Windows computer using VS Code.

4. Make sure all required dependencies are listed in package.json.

5. Make sure there are clear npm/pnpm scripts for:
   - running Identity
   - running Property
   - running Revenue
   - running Connector
   - running all services together if possible
   - typecheck
   - build

6. Make sure no Replit-specific functionality is required for the actual APIs to work.

7. Do NOT remove the existing API endpoints.

8. Keep these ports:
   Identity = 3001
   Property = 3002
   Revenue = 3003
   Connector = 3004

9. Keep the existing API routes:

GET /identity/citizen/:id
GET /identity/health

GET /property/owner/:ownerRef
GET /property/health

GET /revenue/taxpayer/:taxpayerId
GET /revenue/health

GET /connectors
POST /connectors
GET /connectors/:id
PUT /connectors/:id
DELETE /connectors/:id
GET /connectors/health

10. Create/update the README with exact instructions for cloning and running this project locally in VS Code on Windows.

11. Add a .gitignore suitable for Node.js/TypeScript projects.

12. Make sure .env files containing secrets are not committed.

13. Do not introduce a database or other unnecessary technologies.

14. After making the changes, tell me exactly which files were changed and how Member 1 or Member 4 should integrate this module into their main project.

Do not change the architecture unnecessarily. The goal is to make the existing working Member 5 MVP portable from Replit to VS Code/GitHub.